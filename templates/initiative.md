# Template: Initiative

> Use for a durable capability or strategic theme spanning clients and quarters. Created rarely. No parent. No title prefix. The body below goes in the Jira **description** (Markdown, no surrounding code fence). Modeled on AW-152 / AW-153.

**Type:** Initiative · **Parent:** none · **Labels:** `DataScience`, `Modeling` (+ specific) · **Status on create:** Backlog (or In Progress if active)

---

**Summary (title):** `<Capability name>` — e.g. *PyMC Bayesian MMM v0.19*

**Description:**

```
### <Capability statement headline>

<2–3 sentences: what standardized capability this establishes and the value it provides — transparency, rigor, comparability across clients, governance.>

<1 sentence on how artifacts/activities are tracked and mapped, creating a unified reference.>

* **<Pillar 1, e.g. Data ingestion>:** <what this pillar covers>
* **<Pillar 2, e.g. Model specification>:** <…>
* **<Pillar 3, e.g. Priors>:** <…>
* **<Pillar 4, e.g. Inference / diagnostics>:** <…>
* **<Pillar 5, e.g. Delivery / reporting>:** <…>
```

---

## Checklist before creating

- [ ] This is genuinely a *capability*, not a piece of work (otherwise it's an Epic or Story)
- [ ] Pillars cover the workstreams that Epics will hang off of
- [ ] `DataScience` (+ `Modeling`) labels set
- [ ] Lands in Backlog unless work is already underway
